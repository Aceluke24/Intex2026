# ASP.NET Kestrel (see backend/Intex2026/Intex2026/Properties/launchSettings.json — http profile uses port 5090).
# If you run the API on another port, change this to match (e.g. http://localhost:5000).
VITE_API_BASE_URL=http://localhost:5090
